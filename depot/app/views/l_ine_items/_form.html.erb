<%= form_with(model: l_ine_item) do |form| %>
  <% if l_ine_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(l_ine_item.errors.count, "error") %> prohibited this l_ine_item from being saved:</h2>

      <ul>
        <% l_ine_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :cart_id %>
    <%= form.text_field :cart_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
